# Fixture policy — community linear-mcp (compat: pass)
command: ["/usr/bin/node", "./server/dist/index.js"]
filesystem:
  read: []
  write:
    - "./data/cache"
network:
  allow:
    - "api.linear.app"
env:
  allow:
    - "LINEAR_API_KEY"
    - "HOME"
limits:
  memory_mb: 256
  timeout_s: 300
