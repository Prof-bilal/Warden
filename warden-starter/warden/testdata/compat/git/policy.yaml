# Fixture policy — @modelcontextprotocol/server-git (compat: pass)
command: ["/usr/bin/node", "./server/dist/index.js"]
filesystem:
  read: []
  write:
    - "./data/repo"
network:
  allow: []
env:
  allow: []
limits:
  memory_mb: 256
  timeout_s: 300
