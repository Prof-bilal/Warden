command: ["/usr/local/bin/node", "/app/mcp-test/test-server.js", "/app/test-data"]

filesystem:
  read:
    - "/app/test-data"
  write:
    - "/app/test-data"

network:
  allow:
    - "api.github.com"

env:
  allow:
    - "HOME"
    - "PATH"

limits:
  memory_mb: 256
  timeout_s: 300
